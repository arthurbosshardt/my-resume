/**
 * Mobile/desktop split, in CSS pixels.
 * Matches Tailwind's `lg` breakpoint (1024px) — the iPad-landscape /
 * small-laptop convention used as the mobile/desktop line by most
 * modern component libraries. Keep this in sync with the `max-width`
 * value used by the "Mobile" media query tier in every component
 * stylesheet.
 */
export const MOBILE_BREAKPOINT_PX = 1024;
